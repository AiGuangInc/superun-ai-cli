/** 后续菜单统一由宿主原生提问工具收答。@author xiuyu.yi */
export const USER_CHOICE_INSTRUCTION =
  '后续引导中的选项需要用户决定时，必须从当前宿主实际提供的工具列表中识别并调用原生选择能力收答，遵守该工具声明的参数、题型和容量限制。先展示结果和必要说明，再将本轮满足 when 条件且允许展示的选项汇总为同一道单选题，不为每个 nextAction 分别弹框。保留原选项名称、说明与动作映射；超过工具选项上限时按宿主能力分页，完整保留所有选项，不截断、不合并成套餐、不擅自新增业务选项。纯文本菜单或“请回复”不算已完成提问，不能展示后直接结束。仅在工具确实不可用、实际调用失败或分页仍无法表达题型时，说明原因并用完整文本收答；题数或选项数超限应先分页。用户已有明确选择或授权时直接沿用，不重复弹框；自由输入的新需求同样有效，不强制先选菜单。未取得真实答案时保持等待，不把推荐、预选、关闭或超时当作答案。';
